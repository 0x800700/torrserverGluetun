# TorrServer + Gluetun (Cloudflare WARP)

Готовое Docker-решение для запуска **TorrServer** с выходом в интернет через **Cloudflare WARP**, предназначенное для использования **в локальной сети** (например, с Lampa на телевизоре).

Проект ориентирован на простой и воспроизводимый сетап без необходимости настраивать VPN на клиентах (TV, приставки, браузеры).

---

## 1. Описание

Этот проект позволяет:

- запускать **TorrServer** в Docker
- направлять **весь внешний торрент-трафик через VPN**
- при этом сохранить **доступ к Web UI и API по IP сервера в локальной сети**
- использовать TorrServer как backend для **Lampa**

⚠️ В рамках данного проекта **не рассматривается публикация TorrServer в интернет**.  
Доступ предполагается **только из локальной сети**.

Схема работы:

```
TV / Lampa / Browser (LAN)
        │
        │ HTTP :8090
        ▼
   TorrServer (Docker)
        │
        │ весь исходящий трафик
        ▼
     Gluetun
        │
        ▼
 Cloudflare WARP (WireGuard)
```

---

## 2. Основные возможности

- 🔐 Авторизация TorrServer через `accs.db`
- 🌍 Весь внешний трафик TorrServer идёт через VPN
- 📺 Полная совместимость с **Lampa**
- 🐳 Запуск через **Docker Compose**
- 🧩 Клиентам не требуется VPN
- 🛠 Установка и управление через один bash-скрипт

---

## 3. Для кого этот проект

Подойдёт, если:

- TorrServer используется **дома, в локальной сети**
- Основной клиент — **телевизор с Lampa**
- Не хочется настраивать VPN на каждом устройстве
- Нужен простой и понятный Docker-сетап

Не подойдёт, если:

- вы хотите публиковать TorrServer напрямую в интернет
- вы не используете Docker
- вам нужен сложный VPN-маршрутизатор с политиками

---

## 4. Требования

- Linux сервер / VPS / NAS / mini-PC
- Docker
- Docker Compose (plugin)
- Открытый порт `8090` **в локальной сети**
- Cloudflare WARP PrivateKey

---

## 5. Быстрый старт

Весь проект **целиком живёт в одной папке**.

```bash
mkdir torrserverGluetun
cd torrserverGluetun
curl -O https://raw.githubusercontent.com/0x800700/torrserverGluetun/main/install.sh
chmod +x install.sh
./install.sh
```

---

## 6. Структура проекта

```
.
├── docker-compose.yaml
├── install.sh
└── opt
    ├── gluetun
    │   └── wg0.conf
    ├── torrserver
    │   ├── bin
    │   │   └── TorrServer
    │   └── db
    │       └── accs.db
    └── certs
        └── ca-certificates.crt
```

---

## 7. Авторизация TorrServer

Файл пользователей:

```
opt/torrserver/db/accs.db
```

Пример:

```json
{
  "User1": "Pass1",
  "User2": "Pass2"
}
```

---

## 8. Cloudflare WARP: как получить PrivateKey

Порядок действий **одинаков для всех ОС**.

Важно: `PrivateKey = xxxxxxxxxxxxxxxxxxxxx=`  
**Символ `=` в конце обязателен.**

---

## 9. Управление

```bash
./install.sh
```

---

## 10. Проверка

```bash
docker exec -it gluetun wget -qO- https://ipapi.co/ip
```

---

## 11. Безопасность

Проект рассчитан на **локальную сеть**.  
Сложные пароли актуальны только при доступе извне.
